services:
  app:
    build: .
    container_name: aidiscuss
    restart: unless-stopped
    ports:
      - "3088:3000"
    volumes:
      - aidiscuss-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/prod.db
      - NODE_ENV=production

volumes:
  aidiscuss-data:
